The server side has been completed.
The database can be created with the command 
dotnet ef database update InitialCreate

The client is A HTML file containing the functionality requested but it cannot communicate with the server because of CORS.
It has been written in plain Javascript.